# 複製成 .env 並填入實際值（.env 已被 .gitignore 忽略，不會進版控）
# 自架時用 docker compose 會自動讀 .env；Fly.io 請改用 `fly secrets set`。

# Telegram bot token（跟 @BotFather 拿）
TELEGRAM_BOT_TOKEN=

# Claude Max plan 的 OAuth refresh token
# 從你已登入 Claude CLI 的機器上取得：
#   ~/.claude/.credentials.json 裡 claudeAiOauth.refreshToken 的值
CLAUDE_REFRESH_TOKEN=

# 持久化目錄（容器/雲端建議設成掛載卷路徑；本地可不設）
# DATA_DIR=/data
# CLAUDE_CONFIG_DIR=/data/.claude
